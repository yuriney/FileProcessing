package org.example;

import java.io.*;

public class IOExceptionExample {

//        try {
//            FileReader fileReader = new FileReader("nonexistent-file.txt");
//            // Attempt to read from a file that doesn't exist
//        } catch (IOException e) {
//            // Custom error message to be shown
//            System.err.println("An error occurred while trying to read the file: " + e.getMessage());
//        }
}
